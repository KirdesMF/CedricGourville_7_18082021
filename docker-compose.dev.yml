version: '3.9'
services:
  app:
    build:
      target: development
    image: dev-app

  studio:
    build:
      target: studio
    container_name: prisma-studio
    env_file: .env
    expose:
      - '5555'
    ports:
      - 5555:5555
    volumes:
      - ./prisma:/app/prisma
    depends_on:
      - mysqldb
